{
  "name": "baker",
  "version": "0.13.0-alpha.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "release:prod": "release-it --github.release=true --ci",
    "release:alpha": "release-it --preRelease=alpha --no-plugins.@release-it/conventional-changelog.writerOpts.generateOn --ci"
  }
}
