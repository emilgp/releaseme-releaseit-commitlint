{
    "$schema": "https://unpkg.com/release-it@19/schema/release-it.json",
    "git": {
        "commitMessage": "chore: release ${name} v${version}",
        "tagAnnotation": "${name} v${version}"
    },
    "github": {
      "release": true,
      "releaseName": "${name} ${version}"
    }
  }
